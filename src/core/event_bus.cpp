#include "dota/core/event_bus.hpp"

// EventBus is fully defined in the header (template-heavy). This TU exists so
// CMake has something to compile and so future non-template additions have a
// home.
namespace dota {} // namespace dota
